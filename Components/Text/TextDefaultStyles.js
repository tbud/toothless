"use strict";

import {StyleSheet} from 'toothless_scale';

module.exports = StyleSheet.create({
    normal: {
        fontFamily: 'PingFang SC',
        fontSize: 14,
        color: '#333',
    },
});